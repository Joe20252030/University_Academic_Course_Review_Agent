# ── Core configuration ────────────────────────────────────────────────────────
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0

# ── LangChain ecosystem ───────────────────────────────────────────────────────
langchain-core>=0.3
langchain-community>=0.3
langchain-text-splitters>=0.3
langchain-google-genai>=2.0
langchain-openai>=0.2
langchain-huggingface>=0.1

# ── Vector store ──────────────────────────────────────────────────────────────
chromadb>=0.5
langchain-chroma>=0.1
onnxruntime>=1.17    # local embedding runtime in frozen app (chromadb ONNX path)

# ── Document loading / parsing ────────────────────────────────────────────────
pypdf>=4.0
docx2txt>=0.8        # used by langchain_community.document_loaders.Docx2txtLoader
python-pptx>=0.6     # PowerPoint (.pptx) text and image extraction
pytesseract>=0.3     # OCR on image slides inside .pptx files (requires Tesseract)
sentence-transformers>=3.0  # local embedding in source mode (requires torch)
tiktoken>=0.7        # tokeniser used by langchain-openai

# ── Export ────────────────────────────────────────────────────────────────────
python-docx>=1.0
fpdf2>=2.7           # 2.7+ required for Unicode TTF font support
Pillow>=10.0         # desktop app icon loading via PIL.ImageTk

# ── REST API (optional — only needed when running the FastAPI server) ──────────
fastapi>=0.100
uvicorn[standard]>=0.23

# ── Desktop UI extras ────────────────────────────────────────────────────────
tkinterdnd2==0.4.3   # pinned: 0.4.3 uses Tcl-8.6-compatible dylibs; newer releases
                     # ship libtcl9tkdnd*.dylib (Tcl 9) which cannot be loaded by
                     # Python 3.13's bundled Tcl 8.6 and breaks DnD in built apps.

# ── Development / testing ─────────────────────────────────────────────────────
pytest>=8.0
